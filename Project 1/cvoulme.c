#include<stdio.h>

int main(){
    int r = 4;
    int h = 20;
    printf("Volume of the cylinder with radius %d and height %d is %f", r, h , 3.14*r*r*h); // this is how respectively we can use int and float datatype in printf function
    return 0;
}
// data cane taken as input from user using sacnf function